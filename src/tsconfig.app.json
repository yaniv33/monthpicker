{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": []
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}

//enableIvy
//$localize is not defined
//npm cache clean --force & ng serve --prod
//ng add @angular/localize
